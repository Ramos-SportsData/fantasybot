name: Run Fantasy Bot

on:
  schedule:
    - cron: '0 7 * * *'
    - cron: '0 15 * * *'
    - cron: '0 16 * * *'
    - cron: '0 17 * * *'
    - cron: '0 18 * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
      - name: Install package
        run: pip install .

      - name: Create tokens.json from Secret
        env:
          FANTASY_AUTH_JSON: ${{ secrets.FANTASY_AUTH_JSON }}
        run: |
          if [ -z "$FANTASY_AUTH_JSON" ]; then
            echo "Error: FANTASY_AUTH_JSON no esta definido en Secrets."
            exit 1
          fi
          echo "$FANTASY_AUTH_JSON" > tokens.json
          python -c "import json; json.load(open('tokens.json'))" || (echo "Error: Invalid JSON in FANTASY_AUTH_JSON secret"; exit 1)

      - name: DEBUG - probe league standings endpoints
        run: |
          python3 -c "
          from fantasybot.api import FantasyClient
          import json
          c = FantasyClient()
          lid, tid = c.default_ids()
          candidates = [
              f'/leagues/{lid}/teams?x-lang=es',
              f'/leagues/{lid}/ranking?x-lang=es',
              f'/leagues/{lid}/standings?x-lang=es',
              f'/league/{lid}/teams?x-lang=es',
              f'/league/{lid}/ranking?x-lang=es',
              f'/league/{lid}/standings?x-lang=es',
              f'/leagues/{lid}?x-lang=es',
          ]
          for path in candidates:
              print('---', path, '---')
              try:
                  r = c.get(c._cmp(path))
                  s = json.dumps(r, ensure_ascii=False)
                  print(s[:500], '...' if len(s) > 500 else '')
              except Exception as e:
                  print('ERROR:', e)
              print()
          "

      - name: Run Bot
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python -m fantasybot agent --execute
